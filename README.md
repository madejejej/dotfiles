# Dotfiles

This repo contains my gitfiles. It uses GNU Stow to apply symlinks, so the repo doesn't need to live in the home directory.


Install symlinks with `make all` and remove them with `make delete`.
